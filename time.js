/* Takes a time in hundredths of a second and returns it as a formatted string
Examples:
timeToString(2999) returns "29.99"
timeToString(8023) returns "1:20.23"
*/
function timeToString(time){
    let str = "";

    return str;
}


/* Takes a formatted string and returns the time in hundredths of a second
The inverse of the first function
stringToTime("1:01:55") returns 6155
stringToTime("2:10.12") returns 13012
*/
function stringToTime(str){
    let time = 0;

    return time;
}

/* Takes a number (represented as a string) up to 6 digits and formats it
Also, corrects the format of incorrectly formatted times
Examples:
formatTime("2345") returns "23.45"
formatTime("10268") returns "1:02.68"
formatTime("10.036") returns "1:00.36"
formatTime("2.456") returns "24.56"
*/
function formatTime(str){
    let formattedStr = "";

    return formattedStr;
}